server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;
  autoindex off;

  # Nur die Root-URL bedient index.html
  location = / {
    default_type text/html;
    try_files /index.html =404;
  }

  # Alles andere: 404
  location / {
    return 404;
  }
}
